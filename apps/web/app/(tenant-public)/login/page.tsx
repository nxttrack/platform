import { PageHero } from "@/components/lovable/page-kit";

export default function LoginPage() {
  return <PageHero kicker="Auth later" title="Login skeleton" sub="Auth en role-routing starten pas in Phase 3." primary={{ href: "/", label: "Home" }} />;
}
