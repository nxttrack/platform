import { RoutePlaceholder } from "@/components/shell/route-placeholder";

export default function InstructorDocumentsPage() {
  return <RoutePlaceholder kicker="Instructor shell" title="Documenten foundation" description="Instructeurdocumenten en protocollen krijgen later storage en rechten." items={["Protocollen", "Bestanden", "Rechten", "Storage"]} />;
}
