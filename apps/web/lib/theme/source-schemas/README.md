# Trusted source schemas

Derived from the user-supplied V4.2 reference package schemas (handoff v3). These validate Package 1.0 / engine journey-3.0 / source schema string 3.0, not the repository numeric schema 3.

Semantic asset/criterion IDs additionally allow dots to preserve the supplied slot catalog; paths remain independently validated. No other schema constraints are relaxed. The bounded evaluator accepts only these repository-owned schemas, never schemas or references from an upload. The original Default 1.1 JSON shape has not been supplied and is not inferred from this dialect.
