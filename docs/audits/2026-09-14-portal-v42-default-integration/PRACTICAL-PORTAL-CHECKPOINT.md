# Practical portal authority and form persistence

The authenticated local parent browser chain passes with real existing routes/actions: Home → Planning → lesson detail → Back; confirmed on-time cancellation creates a credit and remains cancelled on reload; accepting an afzwem invitation keeps the result pending and issues no certificate; sibling planning excludes the other child's lesson; profile name persists after reload; private feedback persists after reload; payments, documents, media, badge and family routes respond200. Fictional fixture only, no provider/payment/mail action.

The profile test exposed a real pre-existing zero-row-success bug: valid authenticated accounts with no profiles row saw saved=1 from UPDATE even though no data was stored. updateParentProfileAction now upserts only the trusted actor ID plus allowed full_name/phone and selects the persisted ID. It leaves email/avatar/roles untouched. The successful21.4s browser rerun starts with a newly seeded account without a profiles row and proves actual durable creation.

Profile and feedback use the existing DirtyForm. Its explicit discard confirmation now participates in Back/Forward, tab and imperative navigation. Submitting is no longer treated as proof of persistence; prevented/failed submissions retain the warning until a successful reset or explicit discard. Clean draft writers are not invoked unnecessarily. Four navigation browser cases PASS, including cancellation of discard and explicit acceptance after a simulated failed manual submission. Typecheck,505 units and auth checks PASS.

Historical failures remain raw: initial seed lacked the required structured resource (fixture corrected, DB guard retained); two browser attempts hit a relocated Next cache's dangling native sharp alias before authenticated route work; the test environment was restored to a real .next directory, retaining cache/static on task-owned tmpfs only. A later test checked Back before lesson navigation finished; it now waits for the actual lesson URL. The real profile persistence failure was corrected in code. The first manual-form assertion accidentally included Next's route announcer; it now identifies the form's specific error. None are retroactively PASS.

Database semantic coverage is in DATABASE-CERTIFICATION.md, including native cancellation/media consent/feedback, transaction/capacity/credit, temporary offerings, billing documents, tenant isolation and rollback. Browser route availability is distinguished from provider execution: no checkout/provider workflow, real document upload or physical mobile keyboard was performed. Final production build, browsers/CI and original Default gates remain separate.
