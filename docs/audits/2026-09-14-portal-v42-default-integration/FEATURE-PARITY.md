# Verified route/action parity

Canonical authority remains the existing repository domain; source prototype stores and role switches were not ported into live routes. This table names the real implementation and the evidence boundary. Test details and exact commands are in TEST-RESULTS.json and the linked checkpoints. Physical devices and provider delivery are not claimed.

| Function | Owner / real route | Verification and boundary |
|---|---|---|
| Parent Home | `app/(portaal)/portaal/page.tsx`, `portal-journey-server`, shared `PortalJourneyScene` | Real scoped family/curriculum; canonical rings, explicit bound imported release, full-height journey, separate practical navigation. Collection/instructor browser sessions and DB integration. Legacy targets retain their existing renderer. |
| Development | `/portaal/ontwikkeling`, `parent-development-page`, shared development components | Read-only skills/history/milestones, canonical sequence, corrections, search/filter/sort, selected ID and camera roundtrip. Seven viewports, 200% text, keyboard/focus. |
| Historical worlds | canonical chapter snapshots/item completions, `portal-journey-server` | Real stage transition captures exact published bound release/art/scores; later binding changes and carryover do not rewrite snapshots. DATABASE-CERTIFICATION.md. |
| Planning / lesson | `/portaal/planning`, `/portaal/lessen/[id]`, `parent-portal-actions` | Real local click→lesson→Back→confirmed cancellation→credit→reload. Existing deadline/timezone, catchup capacity/idempotency/credit commands tested in native/swim/planning SQL and concurrency suites; no replacement booking authority. |
| Temporary / holiday / turbo | `temporary-offering-actions`, existing `reserve_group_offering_seat` | Existing publication/seat holds/capacity/payment modes retained. Billing/offering/holiday SQL and concurrency pass. Actual provider checkout not run. |
| Graduation / diploma | `graduation-actions`, invitation action and `certificate/[id]` | Actual local parent accepts invitation; result remains pending. Finalization stays a separate staff domain command. Real stored fictional certificate bytes served; revoked certificate denied. No automatic diploma from scene/mastery. |
| Inbox | `/portaal/inbox`, scoped message actions/RPCs | Local actual server draft save/reload/second session/conflict/Back/Forward/retry/explicit review+send, archive/restore and item references. Delivery disabled; no email sent. See MESSAGES-CHECKPOINT.md. |
| Payment | `/portaal/betalingen`, billing actions/Mollie boundary | Existing provider service/webhook and native billing contracts retained, immutable invoice issuance and real PDF route tested, draft PDF refused. Actual standalone webhook, provider snapshot validation, settlement/failure/idempotency and parent reload checked with an explicitly loaded loopback HTTP stub. Live/sandbox provider checkout NOT RUN. |
| Media | `/portaal/ontwikkeling/media`, `participant-media`, private parent/child media routes | Real private stored image bytes, no-download rule, consent-blocked/withdrawn/expired media denial, child approval and sibling isolation tested through standalone HTTP routes. No remote upload or provider scanner claim. |
| Badges and collections | `/portaal/ontwikkeling/badges`, child badges, private collection RPCs | Logical earned stable keys and immutable historical badge release metadata retained. Cosmetic discovery requires separate explicit save; DB retry/concurrency/rollback and parent/child browser persistence pass. Never scores/diplomas. |
| Documents | `/portaal/documenten`, private document/invoice/certificate routes | Actual bytes and private headers checked; unauthenticated access, pending scan, draft invoice and revoked certificate rejected. Local prevalidated fictional file fixtures are not evidence of live malware-scanner operation. |
| Feedback / profile | `/portaal/feedback`, `/portaal/profiel`, existing server actions | Real feedback score/comment persists; profile upsert confirms an actual row before success, including Auth user without profile. Shared dirty-form guard keeps failed input. |
| Family / guardian context | `/portaal/kinderen`, access links and mutable participant checks | Separate real children; same-family switching and unauthorized subject/tenant/role assertions. No fabricated guardian access or public role switch. |
| Child | `/kind`, `/kind/reis`, agenda/badges/profile | Existing child session/capability contract retained; ChildSafe projection exposes no parent inbox, finance or internal assessment note. Real independent child session sees confirmed assessment/explicit compliment/collection only. |
| Instructor | `/instructor/student/[id]`, reviewed assessment actions | Private revisioned drafts, actual canonical save after review, conflict/revocation/concurrency and network recovery. Explicit child compliment is a separate publication. Parent and child independently reload the final score. |
| Theme library | `/platform/themes`, import/editor/private preview/release/binding actions | Source validation, persistent import/revision/review/publish, explicit assignment, fork/restore/export/reimport and recovery/cleanup verified in DB and actual authenticated browser. Published release immutable; no assignment on publish. |
| Test preview | guarded theme preview + APP_ENV=test harnesses | Same scene renderer, fictional read-only view model; no source prototype role bypass or mutable credential injection into iframe. Unauthenticated/private preview checks and source/auth audits. |

The neutral Default fixture is visibly labelled missing artwork. Real reference Parelroute/Ocean packages retain their own identity. There is **no claim that authentic Default 1.1 was imported**. The 52 optional support slots are catalogued explicitly in SUPPORT-SLOTS.md and preserved by the editor/export; they are not silently reported as rendered.
