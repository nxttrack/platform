# Blockers and verification limits

## B01 — ORIGINAL_DEFAULT_ASSETS_MISSING

No authentic `config/manifest.json`, `config/assets-manifest.json`, six Default anchor maps or 96 clean PNG/WebP originals were supplied in the v3 handoff or relevant workspace/attachments. The PDF contains reference evidence, not source artwork. ASSETS.json keeps absent source hashes null.

The original Default 1.1 dialect and six-world art/source acceptance remain BLOCKED. Recognized missing/corrupt Default inputs cannot silently become guided imports. No PDF crops, invented anchors, replacement artwork or renamed Ocean world were used. Existing built-in Default is not rebranded as 1.1. No new authentic Default runtime release was published.

Independent contracts, app/domain workflows, real local storage, editor/import/recovery and migration certification are implemented. Optional support slots are explicitly catalogued and retained, not rendered; see SUPPORT-SLOTS.md.

## Verification limits (not passed by implication)

- Local Firefox/WebKit launch failed because this host lacks GTK/Cairo/GStreamer libraries. Canonical GitHub CI installs all three engines; its actual result must be recorded independently.
- Physical Android/iOS keyboard, safe-area and device performance checks were not run. Browser viewport/keyboard-area emulation is distinct evidence.
- No real or sandbox payment provider, mail/SMS provider, live malware scanner or remote storage/database operation was invoked. Existing payment/webhook contracts and real local invoice issuance/PDF bytes were checked; actual webhook/parent reload was also checked with a loopback snapshot stub; live/sandbox checkout remains NOT TESTED.
- No production operational certification or deployment is implied by this review prototype.
