# Import recovery and published curriculum binding

Canonical baseline `afb53f6e4f72ac8fb0cba88ab98e43373fc7ed80` remains unchanged. Full V4.2 implementation/validation is IN_PROGRESS.

An interrupted/rejected source can be reanalyzed from its verified original bytes as a **new attempt**, linked through `retry_of`; the previous status/error/source hash remain historical evidence. The requested runtime version survives interruption. Missing or changed source bytes require the original upload again. Rate and archive limits remain intact. Upload failure refreshes the stored history; unmount cancels the pending browser upload without retaining callbacks to the removed editor.

Explicit cleanup only accepts terminal rejected imports without current or historical release-source references. It removes bounded, validated keys from the separate private quarantine bucket: the raw source and temporary previews. It never touches `portal-theme-assets`. The durable cleaning/cleaned states can resume after storage interruption or loss of the final confirmation. Concurrent operations preserve exactly one start/finish audit pair. Import metadata is retained after file removal. Running, analyzed and draft records cannot enter cleanup; historical release/revision references block cleanup even if an import status were mislabeled. Extraction itself stays memory-bound; no temporary unpacked filesystem tree is left behind. Validated delivery assets remain insert-only and conservatively retained, including unused draft-version deliveries; broad runtime asset garbage collection is deliberately absent to preserve revision/snapshot/collection references.

Forward migration `20260914145431_portal_theme_import_recovery.sql` adds these recovery fields/private audit records/service-only commands. It also requires a published curriculum at the world-binding database insertion boundary, matching the existing management UI; the version is locked during that check. Historical migrations and function signatures/grants are unchanged. An unpublished version is rejected in the real DB suite; normal binding/concurrent replace/full rollback still pass.

Actual results:

- Typecheck and all 504 units PASS, migration157 and RLS256-public-table audits PASS; SQL lint over all local app schemas reports no errors.
- Existing real-storage theme integration plus recovery contract PASS: terminal-state authorization, scoped source key, concurrent begin/finish once, restart/resume, immutable error/hash history, real release/revision-source reference protection and direct authenticated denial. Existing publication/storage checks, world binding, cosmetic collection and Default provenance protection remain green.
- Authenticated Chromium recovery flow PASS: valid stored source becomes a new analyzed attempt with the requested version; original failed row survives reload; missing source returns a useful error; explicit cleanup removes the failed source and survives reload, while the other analyzed attempt remains usable.
- Existing library browser run: 3 PASS / 1 FAIL. Import/editor/review/publication, guided image mapping and version-copy/revision-restore passed. The binding test checked optional mode buttons before the newly selected tenant page loaded, so it skipped platform opt-in and attempted a disabled button. Its failure-artifact write also hit host ENOSPC; no missing artifact is claimed. Waiting for the target URL and visible management section fixes the test ordering. The original fixture then passed the complete binding/replace/rollback test in `theme-binding-wait-for-tenant-browser.log`; an explicitly selected isolated published fixture independently passed too. The original failed log is preserved.

Evidence logs/hashes are in TEST-RESULTS; `evidence/import-recovery.png` is the actual browser result. The optional authenticated tests do not run without fictional local credentials; they are not evidence of an ordinary CI authenticated run. Original Default source/artwork remains missing (B01); these fixtures never establish its visual acceptance.

Candidate now has **157 migrations**; artifact maximum `20260914145431`, fingerprint `ec8535f2f83642e679a309bb3a4992af96581cf420e055abb2001069495157cd`. Runtime contract remains5. Only the owned local development schema was updated; official history remains151 pending final fresh/upgrade certification. No deployment, remote DB change or provider action.
