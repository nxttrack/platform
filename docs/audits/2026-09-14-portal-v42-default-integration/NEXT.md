# Continue full V4.2 implementation

IN_PROGRESS. User authorized full integration. Continue implementation/tests/feature PR, not a checkpoint-only final response. B01 original Default 1.1 source JSON/anchors/96 artworks is still absent and blocks only those source/visual checks; continue independent work. No invented original schema or assets.

Only worktree /home/codex/repos/nxttrack-portal-v42-default-integration, branch codex/portal-v42-default-integration. Preserve original /home/codex/repos/nxttrack dirty instructor/android/docs work. Canonical main afb53f6e4f72ac8fb0cba88ab98e43373fc7ed80. Feature code through 8d849b8147b08e9b5e40ef859e9c506321742040. Logical commits, feature push/PR authorized; no push/PR yet. No merge, deployment, remote DB changes, provider actions or real communication. Journey Bot retired. No subagents authorized. Dutch progress updates ~60sec.

Read THEME-EDITOR-CHECKPOINT.md, DEVELOPMENT-CHECKPOINT.md and GEOMETRY-HISTORY-CHECKPOINT.md for completed implementation and honest evidence. Current 500 units/typecheck PASS; real swim canon nine SQL suites PASS; local schema lint, migration153/RLS254 PASS. Five Chromium rich scene tests + separate geometric registration test PASS; earlier Development+rich seven PASS including seven viewports and 200% text. Firefox/WebKit could not launch due missing host libraries (14 failures), not PASS. Original artwork visual approval still blocked B01.

Completed: strict native3+richsidecar adapters/ZIP/raster verification; durable quarantine/import/review/publish/export; guided manifestless mapping/pre-draft preview; library editor assets/worlds/guide/collectibles/tokens/fontcontrols; clone versions, immutable revision history/diff/review invalidation/restore; real explicit tenant/program/version/stage world binding with opt-in management/legacy coexistence/concurrency/whole rollback. Native schema remains numeric3. Default cannot publish until verified source dialect exists; no adapter currently fabricates it. Shared parent/child Home and child /kind/reis renderer, Development three tabs/filters/search/dialogs/history/milestones, safe historical snapshot projection, saved historical artwork/binding resolution, child instruction videos retained. Canonical latest observations/mastery/unknown/sequence, separate badge events, smooth registered route and camera/page/access list. Canonical history now paginates beyond response caps; incomplete pages fail closed.

Migrations 153 (151 baseline unchanged + 20260914101412 + 20260914104650). Artifact 07647743fdb7684bf1af810dbf08f223b7a06048964586f74da0018ccfe53b88, runtime contract5 unchanged. Local DB has both dev schemas but migration history still151. Snapshot capture forward function now adds visibility provenance/frozen metadata and canonical recency/completion sequence. Historical stored rows unchanged; legacy ratings without proven audience remain unknown. Real transition/carryover/retry suite covers this. Rich world binding chapter transition itself still needs end-to-end SQL coverage.

NEXT: messages + instructor chain.
- Read source docs05 and AC047-058, reference v4.js already read in prior iteration. Existing actions communication-hub-actions.ts create/reply use service client, require private shell, tenant/guardian/instructor permission and human confirmation, then multiple nontransactional writes + notifications. NewThreadForm/ThreadReplyForm use DirtyForm, which loses dirty state on submit and has no durable drafts. Parent inbox uses AdminActionDrawer with no initial deep-link dialog. ThreadWorkspace constructs `${baseHref}?thread=...` even when baseHref already has kind query: fix while integrating. Parent Development canAsk currently false; do not enable until a real authorized item/lesson context flow exists.
- Implement scoped durable composer drafts with explicit privacy/retention, no unguarded localStorage. Existing messages.status=draft is visible under thread policies, so cannot use it as private unsent storage without semantic/RLS changes. Prefer dedicated actor-owned draft authority for new-thread and reply contexts, optimistic revisions and expiry; child restricted session RLS. Existing helper app_private.current_user_can_view_message_thread can inform auth but write/guardian-view_only/instructor-reply settings must still be enforced. Existing participant_contact_drafts are NOT sendable message drafts, do not conflate.
- Validate actual references (tenant+participant+curriculum item/lesson) on server and store authorized IDs, never trust labels or arbitrary related payment/group IDs. Preserve errors/input/scroll; confirmation only explicit send. Real local test error/retry and context isolation. Consider atomic/idempotent send to avoid duplicate message on retry after response loss; preserve existing public/internal visibility/notifications contract.
- Instructor current save action finalize_swim_assessment sends generic positive label and auto parent notification for visible scores. Need explicit score draft→review→save concurrency through existing RPC; internal note/parent message/explicit child compliment separate and tested in real parent/child sessions. Do not adopt reference fake store/canned five-star mastery.

Remaining also: collectible persistence independent of curriculum (idempotent child capability, historical immutable asset ownership, no score effect); failed/retry quarantine lifecycle and safe orphan cleanup; full portal dialog/action parity; guide motion/celebration and touch/200%/3-engines complete tests; real parent/child runtime assigned theme and historical release rollback proof; final fresh+canonical upgrade+partial/rollback both DB profiles/all suites; frozen install/audit/truth/Lovable/runtime/auth/build/standalone/CI; feature push/PR + Codex review, no merge/deploy. Original Default art stays blocked unless provided.

Owned local runtime: Supabase /tmp/nxttrack-v42-canonical; API58521 DB58522 Docker supabase_db_nxttrack-v42-canonical with tmpfs1GB; local creds /tmp/nxttrack-v42-evidence/canonical-local-env.json and fictional browser manager theme-browser-user.json (0600, never print). Next dev currently running localhost58410 from start-theme-server.py (session66885), fictional env only. Own .next/dev/cache symlink to /dev/shm/nxttrack-v42-dev-cache; TMPDIR=/dev/shm/nxttrack-v42-browser-tmp for browsers (root disk~0.3GB, /dev/shm~2.8GBfree). No global cache/volume pruning, no sudo. Temporary apps/web/playwright.v42-local.config.ts untracked intentionally: existing journeyconfig with webServer undefined and baseURL localhost58410, REMOVE before commit/final. Restore own next-env.d.ts generated changes if present. Do not confuse current dev schema application with official migration proof.

Source root /tmp/nxttrack-v42-handoff-v3/NXTTRACK-V4.2-Codex-Overdracht-v3. All start/master/README/policy/docs00-10/83ACs inventoried; reference v4.js/development/messages read, journey-geometry route/collision portion read. Both PDF texts/select PNGs in /tmp/nxttrack-v42-evidence, tools /tmp/nxttrack-v42-pdf-tools; authentic Default assets absent. Next review rest of source geometry motion semantics if implementing guide travel.

Use original repository final gates. Local helpers /tmp/nxttrack-phase05-evidence for DB profiles. Official migration wrapper RUN_DB_MIGRATIONS=true DATABASE_URL=loopback?sslmode=disable DB_MIGRATION_REPAIR_EXISTING_SCHEMA=false pnpm db:migrate. No real provider/remote writes.

## Active message work after the checkpoint

UNCOMMITTED draft migration created with official CLI: 20260914124643_portal_message_composer_drafts.sql. It is incomplete and has NOT been applied anywhere. Contains private actor/tenant/thread/participant draft table (30-day expiry), explicit grants+RLS+child restriction, thread curriculum_item_id FK, context authorization helper, optimistic save and discard RPC. NEEDS review/tests and atomic send/context-reference integration, fingerprint update after final migration set. Current files physically154 but certified candidate is still previous153; do not report154 as tested. No UI/server adapter added yet. Check lesson membership statuses in context helper and own-context read policy (currently membership/thread check).

Key discovery: EXISTING app_private.reply_native_message_thread and public.reply_native_message_thread in 20260802230000_native_mobile_commands.sql already perform actor/guardian/instructor checks, idempotency via domain_command_receipts, atomic message/thread/read-state writes and communication.thread_replied outbox event. Prefer reuse for public replies from composer; don't reinvent that authority. A wrapper can atomically consume the saved draft and retain it on ordinary errors. New-thread creation and internal replies still need transactional handling compatible with current web actions; user confirms send, autosave never sends. Native RPC currently rejects closed/archived threads and needs true instructor reply setting; outer new helper also checks active roles and child restriction. Existing web action notifications must not double-send when integrating outbox/idempotency. All testing stays fictional loopback.

Draft migration DDL and anonymous guard were tested inside BEGIN/ROLLBACK on owned local DB (message-draft-ddl-probe.log), PASS. No persistence/schema-history change. Authorization comparisons in new helper coalesce nullable guardian IDs to false; keep fail-closed. p_session membership constrained active/trial after probe; retest final. Native helper itself uses nullable booleans; inspect when reusing to ensure wrapper guard and direct RPC don't leave an access gap. No new sender implementation written yet.

### Latest message state (supersedes the earlier not-applied note)

The incomplete 20260914124643 migration is now manually applied ONLY to the owned local dev DB (history remains151). Added send_message_composer_draft, public.messages.reference_json and atomic new thread/reply handling with scoped command receipt/replay, current revision/operation guard, explicit confirmation, authorized item/lesson reference snapshot, private draft deletion in the same transaction. Public reply reuses app_private.reply_native_message_thread and its outbox; internal branch creates no public outbox. First SQL syntax typo in nested JSON was fixed; failed delta transaction rolled back, fixed delta applied. No remote action. This remains UNCOMMITTED, artifact assertion still153/old fingerprint pending final154 migration shape.

NEW tests/integration/portal-message-composer.test.ts (also uncommitted) actually passed on local DB: author-only draft RLS even against tenant admin, tenant/participant/thread isolation, unassigned/null-guardian denial, visibility/forged-reference rejection, optimistic concurrent saves on two connections, reconnect, expiry/discard; new-thread ordinary failure via local message insert trigger rolls back thread/message/outbox and keeps draft; explicit confirmation/revision denial; successful retry produces exactly one message/outbox; same operation replays; other actor cannot replay; existing thread follow-up works. Evidence /tmp/nxttrack-v42-evidence/message-draft-first-db.log and message-composer-atomic-db.log (1 broad test PASS). Fictional randomUUID@example.test fixtures remain only in owned DB, no real delivery, outbox workers off. Need still child-restricted-token, delegated view_only, instructor enabled/disabled/internal, real item/session valid+cross-child refs, concurrent send, archive/replay permission tests.

Need next add read_message_composer_draft RPC resolving/validating actual reference labels and preserving any existing same-child new-thread draft context (do not silently replace unsent context from query labels); server action adapter (same existing private shell + authenticated Supabase RPC + known error mapping/classifyContent); shared controlled composer with debounced serialized saves, no localStorage sensitive text, review+explicit send, errors preserve input, scope keyed user/tenant/thread/participant, restore after reload. Parent inbox dialog initial query and Development canAsk only after functional. For default new thread parent actor receives self as guardian, staff/instructor uses actual primary guardian; UI must show that recipient before confirmation. Existing admin full metadata creator should retain functionality.

Potential gotchas discovered: ThreadWorkspace appends ?thread to baseHref that may already contain kind; use a preserving URL helper. getScopedCommunicationHub parent threads currently only filters guardian_user_id and later loads visible participants; new reference rendering must exclude revoked participant contexts, not leak labels through raw thread rows. Existing web create/reply notifyThreadRecipients after writes, native reply only inserts communication.thread_replied outbox (no TS handler found by rg). Preserve notification behavior once, without duplicate sends on replay. Do not enable external mail/jobs in local tests. Existing updateMessageThreadAction is ADMIN ONLY, so parent archive/restore must use per-user participation state or a properly authorized existing service rather than granting parents global thread-status/assignment authority.

Read RPC read_message_composer_draft added and dev-applied after send tests; resolves authorized reference labels and recipient, resumes saved reference rather than accepting a query relabel. Not yet exercised in server/UI. Current candidate files154 and artifact pin now20260914124643 / 06a9642917a6c7c85ac7b0f54af8cdf409d2bd63252c22b6b51ee2b5c871d908; runtime contract5 stays. Local migration history remains151, so final official upgrade/fresh proof still pending. Latest SQL lint before read RPC PASS no schema errors. No final schema certification claimed.

Current frontend adapter work: added uncommitted message-composer-contract.ts and message-composer-actions.ts (load/save/discard/send, strict ID/length/enums, existing private shell/authenticated RPC, known Dutch error mapping, server classification, response-safe retry and URL helper preserving kind). Initial typecheck PASS (message-composer-actions-typecheck.log); not wired to UI yet. send action now also requires expectedGuardian UUID/null. New DB send signature adds p_expected_guardian optional8tharg to prevent primary-guardian changes between review and send; guard dev-applied after atomic test. Parent sends use null (school), staff/instructor use loaded recipient.id. Old seven-arg dev function was dropped before new signature; fresh migration contains only new signature. Need regression test changed guardian. Rerun local atomic test requested next includes new read RPC assertions. No notification bridge hooked yet.


## Messages committed; next instructor chain

Message implementation committed as e691cecc5cadd48042d41667cd80e34554017955. Read MESSAGES-CHECKPOINT.md. 502 units, typecheck/auth, migration154/RLS255, local SQL lint, two broad message DB contracts and all nine canonical swim DB suites PASS. Final real parent Chromium flow PASS with reload, two-context optimistic conflict, ordinary network failure/retry, recipient confirmation, exact authorized item reference, single message, archive/restore and sibling isolation. New seed helper scripts/db/seed-portal-message-browser.mjs is repository-backed and explicit-loopback-only; fixture JSON0600 stays outside repository. Local latest fixture /tmp/nxttrack-v42-evidence/message-browser-final-fixture.json. No external action. Original SQL/history151 untouched; dev schema154, official final migration proof pending. Artifact pin still 20260914124643 /06a964...908, contract5. Five public message RPCs now SECURITY INVOKER wrappers around private SECURITY DEFINER commands; direct native reply shares explicit guard. Parent/new and all reply composers wired; admin full new-thread metadata form retained. Read/update visibility and parent mutableParticipantIds preserved. Instructor new parent-message button is not wired yet. Need immediate browser-back-during-failed-save coverage; link/context/dialog/archive flush exists, beforeunload protects hard navigation. Physical expiry prune only next author's save, not automatic day30 physical deletion.

NEXT INSTRUCTOR: apps/web/app/(instructor)/instructor/student/[id]/page.tsx:253-336 currently canonical+legacy score form calls scoreProgressItemAction in instructor-actions.ts:296; canonical path finalize_swim_assessment, then AUTOMATIC parent notification and generic childVisible compliment checkbox. Replace canonical UI with real draft -> review -> finalize, retaining legacy compatibility. Existing swim_assessment_drafts table already has rating, note, visibility, context_json, client_operation_id, draft_revision, saved_by_user_id, expiry30days and actor/item unique index; existing RLS. No need another draft table. Existing app_private.finalize_swim_assessment locks enrollment, checks permission, idempotency, appends immutable observations/corrections; use it behind an optimistic current-observation check under the same enrollment lock. Latest order observed_at/finalized_at/id, retractions excluded. New wrapper/draft commands only forward migration155 AFTER154, no old migration edits. Parent notification must not automatically send on score-save. Parent message and explicit child compliment are separate actions; new simple new-message dialog can be reused for instructor but must disallow general new context and verify pupil. Child compliment needs an explicit published source rather than blindly setting generic childVisible on score-save; old historical childVisible must remain compatible. Current canonical parentJourney and childPortal builders consume observation.positive_label and context_json.childVisible; inspect safe projections before adding separate compliment authority. Private note must not appear in parent/child payload. Actual parent/child/instructor sessions plus conflict/failure/retained scroll still required.

CI .github/workflows/ci.yml only static/build/audits/browser (no database service). New authenticated message test explicitly SKIPS without fixture; local PASS is real but do not claim it ran in default CI. Final available browser matrix and all local DB profiles still required. No pushes/PR yet. Full integration continue, do not final at this checkpoint.

Root disk constrained (~0.15GB); own /tmp/nxttrack-v42-pdf-tools and /tmp/nxttrack-v42-handoff-v3 were moved to same-named /dev/shm directories with original paths symlinked. Own Next cache remains tmpfs, TMPDIR for Chromium remains /dev/shm/nxttrack-v42-browser-tmp. Current Next server still session66885. No other worktree/cache/DB touched.

## Active instructor implementation (uncommitted)

Created through official CLI: `supabase/migrations/20260914134016_reviewed_instructor_assessments.sql` (candidate155). Reuses canonical swim_assessment_drafts with review_owned/base_observation_id/correction_reason/session_id columns, private read/save/discard/finalize implementations plus public invoker wrappers. Guard checks child restriction, real swim permissions, exact enrollment/item/published version and optional exact lesson membership/teacher; latest observation query matches retracted/corrected filtering then observed_at/finalized_at/id. Canonical enrollment lock serializes save/finalize; immutable finalization reuses app_private.finalize_swim_assessment, expected predecessor comparison, actor receipt/replay, no implicit note/childVisible/notification. Foreign client draft is rejected, not silently converted. Save rechecks context after enrollment lock to avoid version race. Existing finalizer signature/history preserved. NO server action or UI connection yet, NO explicit child compliment authority yet; this migration is incomplete and uncommitted.

DDL was verified in BEGIN/ROLLBACK, then the entire155 schema was dev-applied transactionally to OWN loopbackDB. Later helper hardened published-version/context check via local CREATE OR REPLACE transaction. Migration history STILL151; runtime artifact assertion STILL154 until new migration shape is final. Do not claim full155 certification. Tests/integration/instructor-assessment-review.test.ts uncommitted: one broad real DB test PASS in instructor-draft-context-db.log (INSTRUCTOR_REVIEW_TEST_DATABASE_URL=loopback pnpm exec tsx --test ...). It creates two qualified assigned instructors, real published curriculum (fixture seeding only), confirms draft private/no early observation or notifications, explicit confirm, first finalize/replay, second editor stale predecessor refusal with draft retained, and deliberate correction yielding immutable two-observation history with no note/childVisible notification. Initial fixture failures: 'assistant' unsupported role (fixed support), missing verified qualifications (added legitimate fixture qualification rows), unpublished curriculum (published fixture; newguard also rejects unpublished). These were fixture corrections, not disabled safety checks. Need simultaneous connection race, storage fault rollback, child/revoked assignment/cross-context permissions, legacy draft coexistence, then real parent/child/instructor browser chain.

Next: implement thin typed authenticated instructor draft actions and client review card on canonical student assessment tab, preserve legacy form and scroll/context. Existing currentObservationByItemId uses Map of all effectiveObservations and can overwrite latest with older entries: use latestJourneyObservations. New public send_review RPC directly reuses native finalizer, but old scoreProgressItemAction canonical branch still does auto parent notification and childVisible; replace canonical route flow + remove accidental obsolete alternate canonical submission path or route it to review gate. Parent messages should use new MessageComposer/NewMessageDialog (instructor new dialog must require participant and show authoritative guardian). Child compliment must be a separate explicit persisted publication, never a score-save checkbox; old legacy childVisible history remains compatible. Proposed explicit compliment row linked to immutable observation, service-projected allowlisted text, no note/actor leak, no rating change; implement in155 before finalizing fingerprint. Builders in child-portal.ts:493, portal-journey-view.ts:72, portal-development-view.ts:67 currently derive child positive label from context_json.childVisible + positive_label. Parent uses generic assessment label. Need curated compliment separately without rewriting original observations.

Badge discovery: evaluateBadgeTriggerSet uses award_badge_batch RPC for automatic criteria events, does NOT call the old awardBadge/notify function that can email. Batch SQL creates in-app tenant_notifications + badge_batch_notifications queued, canonical awards/receipts. Preserve actual automatic badge evaluation after confirmed score; don't call createParentNotificationsForParticipant on score-save. Inspect downstream delivery before asserting no automatic email. Old canonical completedCount uses projection >=0.8; do not introduce that hardcoded threshold into new flow when real per-item mastery_threshold differs. Canonical finalizer emits observation outbox and recomputes projections. Any new effects need response retry/idempotency and ordinary-failure truthfulness.

### Instructor active state after compliment/card work

155 now also adds swim_child_compliments (separate explicit publication, immutable text/context; author may anonymize on Auth deletion; child/parent direct table read denied, service projection pending), private/public publish_swim_child_compliment. Confirmation/current canonical observation/teacher permission/child restriction enforced; exactly one publication per observation and actor operation retry. New table + read RPC returning current `compliment` metadata DEV-APPLIED transactionally to owned DB. No original observation/score/note/childVisible mutation or notification. Broad instructor DB test extended and PASS: instructor-explicit-compliment-db.log includes parent denial, no-confirm refusal, stale observation refusal, same operation replay, different overwrite rejection, original observations unchanged, immutable wording and zero notifications. Still only one broad test; simultaneous true concurrency and fault injection not covered yet.

New UNCOMMITTED files: instructor-assessment-contract.ts/actions.ts (typed actor+tenant+participant+enrollment+item context, load/save/discard/finalize/publish via authenticated RPC; actions typecheck PASS instructor-review-actions-typecheck.log). Finalize action currently only refreshes paths after canonical RPC: automatic badge evaluation integration is STILL TODO before wiring final product. Old canonical scoreProgressItemAction still old behavior. Curated child DTO projection not wired yet.

New UNCOMMITTED ReviewedAssessmentCard at components/assessments/reviewed-assessment-card.tsx: scoped remount, initial load generation, controlled FivePointAssessment, debounced server drafts, review/confirmation, conflict comparison with explicit new baseline adoption, retry/discard, separate explicit compliment dialog, shared flush guard and beforeunload. Initial card typecheck launched (log instructor-review-card-typecheck.log, session33943; check result). Card NOT WIRED to any page and not browser-tested yet. FivePointAssessment extended with optional controlled onChange, keeping old uncontrolled behavior. Shared message-composer-navigation module renamed to components/portal/draft-navigation.ts; message composer + personal archive imports now registerDraftWriter/flushDraftWriters so assessment drafts share safe link navigation flush. Must run existing message browser regression after this common change. Normalized correctionReason comparison to avoid infinite save loop when DB trims whitespace. Need audit other pending edge cases, especially error/scroll, multiple cards, group assignment revocation, reload/context-switch and pending browser-back.

NEXT immediate: check card typecheck; wire canonical student rows to card and fix latest observation Map. Add role-safe child compliment read/DTO (allowlist message only; keep historical childVisible fallback) and instructor new parent message dialog (requires child, recipient shown). Preserve canonical badge evaluation through existing automatic award_badge_batch; it currently creates in-app batch notifications, no direct createTenantNotifications email call, but verify downstream no automatic email before claim. Remove old canonical score action automatic parent notification/childVisible checkbox route path or force it through review. Old legacy scoring stays compatible. Add real instructor-parent-child browser/DB fixtures, then remaining V4.2 work/final full gates. No final response/checkpoint stop: full integration remains in progress.


## Instructor implementation verified; next collectible/motion/lifecycle work

Read INSTRUCTOR-CHECKPOINT.md. Instructor canonical draft/review/explicit finalize and separate immutable child compliments are now wired and tested. 504 units, typecheck, auth/migration155/RLS256, SQL lint, actual simultaneous/fault/revocation DB contract, nine swim SQL suites, message DB contracts PASS. Actual 3-session instructor/parent/child Chromium PASS (34.4s); repeated original messages Chromium PASS (22.7s). Private note absent from parent/child HTML, child gets curated compliment only after explicit publication; messages independently confirmed; authorized item UUID backlinks work. Scope stays featurebranch only; no push/PR yet. Artifact155 a4aa824fa1e611ac0217354b6f999e372fd686462f7ad278c7c2d4a1868d9b03, contract5. DevDB history still151; final full official fresh/upgrade pending. Temp playwright.v42-local.config.ts still untracked, remove before final. Server66885 localhost58410; browser TMPDIR same tmpfs. Latest instructor fixture /tmp/nxttrack-v42-evidence/instructor-browser-fixture-5.json; used fixture must NOT be rerun expecting empty score. Seed a new file using loopback-only helper.

Continue entire independent integration: collectible persistence (pool random per theme, explicit save, historical asset/title, tenant/child isolation, no curriculum effect); guide motion/celebration; importer rejection/retry/quarantine cleanup; practical route parity and full final acceptance/build/DB/browsers. Need immediate browser Back with failed unsaved draft and durable badge retry limit documented. Original Default still B01. Do not finalize at this checkpoint.

Collectible investigation: existing child portal context v1 capabilities exact8 and old/native clients must remain compatible. Suggested additive presentation-capabilities column on private session context (default empty) and NEW service-only child-start wrapper reusing old initializer, transactionally granting only collectibles.write_safe in separate column. Existing returned v1 context/capabilities and native start stay unchanged; new cosmetic command must check actual active child session + new capability server-side. Parent actions require actual allowed mutable child and unlocked parent session. No public/authenticated DB mutation grant. Collectible offers server-random from current authorized immutable published presentation pool; explicit save idempotent, unique child/theme/stable item, historical title/asset kept, no business record writes. No implementation exists yet. Source v3-ui.js:47-64 is reference for random nonrepeating pool, reveal then explicit save, independent collection. UI needs outside-route discovery and accessible collection list.
